id z = z.

[Num a => a -> a]
foo = {
  id id
}.


bar = id 5.

[U32]
baz = bar.

[Num a => b -> U32]
iop x = 5.


data Maybe a = Just a | Nothing.

data Mop a = Pom (Wer a).

data Wer a = Re a | Im a | Nil.

data Toto a b = Mo (b a).

[a -> U32]
m x = match x {
  Just b => match b {
    Re b => b,
    Im b => b,
    Nil => 5,
  },
  Nothing => 0,
}.
