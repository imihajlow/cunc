data Maybe a = Just a | Nothing.

id z = z.

[Num a => a -> a]
foo = {
  id id
}.

inc x = {
  sum 1 (convert x)
}.

[U8 -> U16]
inc_u8 = inc.

bar x y z = {
  let p = x y;
  p z
}.

[Maybe a -> b]
baz x = x.

zoo = Nothing.

zuzu = let x :: U8 = 5; Just x.

foooo = baz zuzu.
